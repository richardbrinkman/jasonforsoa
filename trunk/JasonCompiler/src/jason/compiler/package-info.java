/**
 * The <code>jason.compiler</code> package currently contains just one class:
 * {@link jason.compiler.JasonProcessor}. The Jason compiler is invoked directly 
 * by javac.
 * The syntax to invoke javac is:
 * <pre>
 *	<code>javac -processor jason.compiler.JasonProcessor -cp ... &lt;src_files&gt;</code>
 * </pre>
 */
package jason.compiler;

